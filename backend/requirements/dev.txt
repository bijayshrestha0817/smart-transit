# Development + testing dependencies.
-r base.txt

pytest>=8.3
pytest-django>=4.9
pytest-asyncio>=0.24        # async consumer tests
factory-boy>=3.3            # test fixtures
ruff>=0.8                   # lint + format
